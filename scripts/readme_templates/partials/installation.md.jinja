{%- set pm_lang_map = {"maven": "xml", "gradle": "groovy", "mix": "elixir", "cargo": "toml", "npm": "sh", "pnpm": "sh", "yarn": "sh", "pip": "sh", "uv": "sh", "go": "sh"} -%}
{%- if install_commands is defined %}
{% for pm, cmd in install_commands.items() %}
{%- set rendered_cmd = cmd.replace('${version}', project.version) if cmd is string else cmd %}
{%- if rendered_cmd is string and '\n' not in rendered_cmd %}
```sh
{{ rendered_cmd }}
```
{% elif rendered_cmd is string %}
```{{ pm_lang_map.get(pm, "text") }}
{{ rendered_cmd.strip() }}
```
{% endif %}
{%- endfor %}
{%- elif install_command is defined %}
{%- set rendered_install = install_command.replace('${version}', project.version) if install_command is string else install_command %}
{%- if rendered_install is string and '\n' not in rendered_install %}

```sh
{{ rendered_install }}
```
{% else %}
{%- set first_pm = package_manager[0] if package_manager else "text" %}

```{{ pm_lang_map.get(first_pm, "text") }}
{{ rendered_install.strip() }}
```
{% endif %}
{%- elif package_manager | length == 0 %}

Build from source as part of the [tree-sitter-language-pack]({{ project.repository }}) workspace.
{%- endif %}

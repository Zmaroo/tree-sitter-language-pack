<!-- Generated by scripts/generate_readme.py. DO NOT EDIT. -->

# tree-sitter-language-pack — {{ name }}

{% include 'partials/badges.html.jinja' %}

<div align="center">
  <a href="{{ project.discord }}">
    <img height="22" src="https://img.shields.io/badge/Discord-Join%20our%20community-7289da?logo=discord&logoColor=white" alt="Discord">
  </a>
</div>

{{ description }}

## Installation

{% if language == "cli" %}
{% include 'partials/installation.md.jinja' %}

Or install via Homebrew:

```sh
brew install kreuzberg-dev/tap/ts-pack
```

{% else %}
{% include 'partials/installation.md.jinja' %}
{% endif %}

## Quick Start

{% include 'partials/quick_start.md.jinja' %}

{% if language in ("rust",) %}
## Features

{% include 'partials/features.md.jinja' %}

### Feature Groups

Enable curated language sets instead of individual languages:

```toml
[dependencies]
ts-pack-core = { version = "{{ project.version }}", default-features = false, features = ["web"] }
```

Available groups: `all` (default), `web`, `systems`, `scripting`, `data`, `jvm`, `functional`.

{% endif %}
## API Reference

{% if language == "cli" %}
### Commands

- `ts-pack init` -- initialize a `language-pack.toml` config in the current directory
- `ts-pack list` -- list all available language names
- `ts-pack download <lang>...` -- download specific parsers for offline use
- `ts-pack status` -- show download status of all configured languages
- `ts-pack add <lang>...` -- add languages to `language-pack.toml`
- `ts-pack clone` -- clone parser source repositories (for development)

### Global Flags

- `--config <path>` -- path to `language-pack.toml` (default: current directory)
- `--verbose` / `-v` -- enable verbose output
- `--help` -- show help for any command

{% elif language == "node" %}
### Language Discovery

- `availableLanguages()` -- list all supported language names
- `hasLanguage(name)` -- check if a language is available
- `languageCount()` -- total number of supported languages

### Parsing

- `getParser(name)` / `parseString(source, language)` -- parse source code into a syntax tree

### Download API

- `init(languages)` -- pre-download specific languages for offline use
- `download(languages)` -- download parsers on demand

### Intelligence

- `process(source, config)` -- extract structured analysis (functions, classes, imports, comments, chunks) from source code

{% elif language == "go" %}
### Language Discovery

- `Registry.AvailableLanguages()` -- list all supported language names
- `Registry.HasLanguage(name)` -- check if a language is available
- `Registry.LanguageCount()` -- total number of supported languages

### Parsing

- `Registry.GetParser(name)` / `Registry.ParseString(source, language)` -- parse source code into a syntax tree

### Download API

- `Registry.Init(languages)` -- pre-download specific languages for offline use
- `Registry.Download(languages)` -- download parsers on demand

### Intelligence

- `Registry.Process(source, config)` -- extract structured analysis (functions, classes, imports, comments, chunks) from source code

{% elif language == "csharp" %}
### Language Discovery

- `TsPackClient.AvailableLanguages()` -- list all supported language names
- `TsPackClient.HasLanguage(name)` -- check if a language is available
- `TsPackClient.LanguageCount()` -- total number of supported languages

### Parsing

- `TsPackClient.GetParser(name)` / `TsPackClient.ParseString(source, language)` -- parse source code into a syntax tree

### Download API

- `TsPackClient.Init(languages)` -- pre-download specific languages for offline use
- `TsPackClient.Download(languages)` -- download parsers on demand

### Intelligence

- `TsPackClient.Process(source, config)` -- extract structured analysis (functions, classes, imports, comments, chunks) from source code

{% elif language == "ffi" %}
### Functions

- `ts_pack_registry_new()` / `ts_pack_registry_free(reg)` -- create and destroy a registry handle
- `ts_pack_language_count(reg)` -- total number of supported languages
- `ts_pack_get_language(reg, name)` -- get a language pointer by name
- `ts_pack_init(reg, languages, count)` -- pre-download specific languages for offline use
- `ts_pack_download(reg, languages, count)` -- download parsers on demand
- `ts_pack_process(reg, source, len, config_json)` -- extract structured analysis from source code
- `ts_pack_free_string(ptr)` -- free a string returned by `ts_pack_process`

{% else %}
### Language Discovery

- `available_languages()` -- list all supported language names
- `has_language(name)` -- check if a language is available
- `language_count()` -- total number of supported languages

### Parsing

- `get_parser(name)` / `parse_string(source, language)` -- parse source code into a syntax tree

### Download API

- `init(languages)` -- pre-download specific languages for offline use
- `download(languages)` -- download parsers on demand

### Intelligence

- `process(source, config)` -- extract structured analysis (functions, classes, imports, comments, chunks) from source code

{% endif %}
For detailed API documentation, see the [{{ name }} package]({{ project.repository }}/tree/main/{{ output_path | replace('/README.md', '') }}).

## License

{{ project.license }} -- see [LICENSE]({{ project.repository }}/blob/main/LICENSE) for details.

---

Part of [tree-sitter-language-pack]({{ project.repository }}) -- {{ project.description }}.
